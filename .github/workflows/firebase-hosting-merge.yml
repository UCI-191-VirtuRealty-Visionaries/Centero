# Based on auto-generated file
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
"on":
  push:
    branches:
      - production
jobs:
  build_and_preview:
    name: Build Flutter (Web)
    runs-on: ubuntu-latest

