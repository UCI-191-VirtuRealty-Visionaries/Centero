import 'package:flutter/material.dart';

class PageDebugPortal extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Debug Portal'),
      ),
    );
  }
}
