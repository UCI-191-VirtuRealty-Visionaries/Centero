import 'package:centero/widgets/page_frame.dart';
import 'package:flutter/material.dart';

class PageTopicResident extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return PageFrame(
      child: Text('$this'),
    );
  }
}